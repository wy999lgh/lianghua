# AI量化交易系统 后端容器镜像
# 构建: docker build -f docker/Dockerfile.backend -t quant-backend .
FROM python:3.11-slim

WORKDIR /app

COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

COPY . .

EXPOSE 8000

CMD ["uvicorn", "app:app", "--host", "0.0.0.0", "--port", "8000"]
